// Core Platform — cross-cutting hooks public API.
// Re-exports existing V1 implementations (no code moved yet).
export { useTheme, THEMES } from '@/hooks/useTheme'
export type { DashboardTheme } from '@/hooks/useTheme'
